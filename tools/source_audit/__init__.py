"""Shared base module for per-kind source-audit HTML surfaces.

Consumed by `audit_figures.py`, `audit_tables.py`, `audit_images.py` (and
designed so `tools/equation_audit/audit_equations.py` could later migrate
without breaking sidecar continuity).
"""
