# software-repository-reconnaissance — Tool Policy

Run `discover_repository.py` first. Use `select_affected_checks.py` only with an accepted project profile and declared changed paths. Read source with targeted search after discovery. Do not run builds, tests, generators, installers, or network commands under this skill.
