# software-repository-reconnaissance — Brief Schema

Required: `ScopePath`, `TaskSkill`, `PackageID`, `Objective`, `AcceptedBasis`, `ReadScope`, and `ExpectedReturn`.

Optional runtime overrides: `PROFILE_PATH`, `FOCUS_PATHS`, `CHANGED_PATHS`, and `MAX_DEPTH`.

Use `ApplyEdits: false`. Any requested follow-up implementation requires a new brief.
