# software-test-planning — Tool Policy

Use discovery only when test surfaces are not already known. Use affected-check selection with project-relative paths. This skill plans checks but does not execute them. Do not invent command IDs absent from the profile; label additional checks as proposals.
