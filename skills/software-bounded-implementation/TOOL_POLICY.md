# software-bounded-implementation — Tool Policy

Use repository-native editors and targeted reads. Use `select_affected_checks.py` to propose checks, `run_registered_checks.py` only for brief-authorized profile entries, and `validate_change_scope.py` before return. Use structured comparison and generated-drift tools only when those artifacts are in scope. Never run unregistered install, release, network, or destructive commands.
