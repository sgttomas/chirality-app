# Extracts Directory

This directory is a placeholder per PROTOCOL requirements. Raw extract files are not duplicated here; the Source_Index.csv provides full paths to all source artifacts under `_Estimates/`. Source snapshots are immutable at their original locations.

See Decision_Log.md DEC-AGG-005 (from prior snapshot) for rationale.
