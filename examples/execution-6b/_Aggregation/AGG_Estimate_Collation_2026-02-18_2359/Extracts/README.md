# Extracts

Raw estimate artifacts are preserved in-place at their source snapshot locations under `_Estimates/`. This aggregation run did not need to extract or transform raw data into intermediate form because all source Detail.csv files passed schema validation and were directly collatable.

Source snapshot paths are documented in `../Source_Index.csv`.
